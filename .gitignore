# Terraform
*.tfstate*
.terraform/
*.tfvars
!*.tfvars.example

# Provider lock files ARE committed (removed from ignore) so others get identical
# provider versions on `terraform init`.

# Tooling
stratus.exe
*.exe
.stratus-red-team/

# Build artifacts (regenerated by `terraform apply` via the archive_file data source)
terraform/logging/lambda/responder.zip

# Evidence — redact before committing
evidence/raw/

# OS
Thumbs.db

# Stray leftovers not yet cleaned up on disk (kept out of git regardless)
/out.json
.delete_test_file
/_to_delete/
/_tmp_extract/
